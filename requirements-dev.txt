# Test and schema-validation dependencies.
# Install with: pip install -r requirements-dev.txt
# Once pyproject.toml exists (Task 1.5), these move to [project.optional-dependencies].dev.
pydantic>=2.0
jsonschema>=4.18      # 4.18+ required for Draft202012Validator
pytest>=7.0
pyyaml>=6.0
