# Evaluation harness wrappers.
# Wraps VLMEvalKit for standard benchmarks (RealWorldQA, MMBench, POPE) and
# implements the custom Stage A eval (photo captioning, VQA) against datasets/.
# All eval code produces BenchmarkScore objects consumed by MetricsReport.
